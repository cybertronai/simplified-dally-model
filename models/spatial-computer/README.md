# Spatial computer (processor at every interval)

This is a placeholder for a future model with a processor at every interval.
Its topology, processor behavior, communication costs, input/output semantics,
resource limits, and instruction-set compatibility are not yet specified.

This model cannot be run or scored until its specification is defined.

See the [models catalog](../) for the specified models.
